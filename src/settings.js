define('settings', {
	vPadding : 10,                       // Vertical padding
	padding : 35,                        // Size of the line-number margin
	language : 'html',                   // Default highlighting language
	width : window.innerWidth,
	height : window.innerHeight,
	font : 'Courier New, monospace',
	highlight : 'rgb(64, 64, 64)',       // Selection color (when text is selected)
	cursor : 'rgb(176, 208, 240)',
	scrollbar : '#BBBBBB',
	background : 'rgb(21, 21, 21)',      // Text editor background
	tabSize : 4,
	fontSize : 14,
	lineHeight : 14,
	mouseSensitivity : 3,                // Factor by which the scrolling value is reduced
	clickTimeout : 1000                 // Maximum time between mouseDowns in a double click
});
